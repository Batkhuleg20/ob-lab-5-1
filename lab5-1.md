# Онлайн номын сангийн системийн класс диаграм

```mermaid
classDiagram
    class Хэрэглэгч {
        +хэрэглэгчийнID: String
        +нэр: String
        +и-мэйл: String
        +нууц_үг: String
        +утасны_дугаар: String
        +бүртгэлийн_огноо: Date
        +бүртгүүлэх()
        +нэвтрэх()
        +мэдээллээ_шинэчлэх()
        +гарах()
    }

    class Ном {
        +номынID: String
        +гарчиг: String
        +зохиогч: String
        +хэвлэлийн_газар: String
        +хэвлэгдсэн_он: Integer
        +ISBN: String
        +хуудасны_тоо: Integer
        +товч_тайлбар: String
        +ангилал: String
        +байршил: String
        +боломжит_хувь: Integer
        +зээлсэн_хувь: Integer
        +мэдээлэл_авах()
        +боломжит_эсэх(): Boolean
    }

    class Захиалга {
        +захиалгынID: String
        +хэрэглэгчийнID: String
        +номынID: String
        +захиалсан_огноо: Date
        +буцаах_огноо: Date
        +статус: String
        +торгууль: Double
        +захиалах()
        +буцаах()
        +сунгах()
        +торгууль_тооцох()
    }

    class Сэтгэгдэл {
        +сэтгэгдлийнID: String
        +хэрэглэгчийнID: String
        +номынID: String
        +үнэлгээ: Integer
        +агуулга: String
        +огноо: Date
        +нэмэх()
        +засах()
        +устгах()
    }

    class Админ {
        +админID: String
        +нэр: String
        +и-мэйл: String
        +нууц_үг: String
        +эрх: String
        +нэвтрэх()
        +ном_нэмэх()
        +ном_засах()
        +ном_устгах()
        +хэрэглэгч_удирдах()
        +захиалга_удирдах()
    }

    class Төлбөр {
        +төлбөрийнID: String
        +хэрэглэгчийнID: String
        +төлбөрийн_төрөл: String
        +дүн: Double
        +төлсөн_огноо: Date
        +статус: String
        +төлөх()
        +буцаан_олгох()
        +баримт_хэвлэх()
    }

    class Мэдэгдэл {
        +мэдэгдлийнID: String
        +хэрэглэгчийнID: String
        +төрөл: String
        +агуулга: String
        +огноо: Date
        +статус: String
        +илгээх()
        +уншсан_болгох()
        +устгах()
    }

    class ЦахимХувь {
        +цахимID: String
        +номынID: String
        +файлын_зам: String
        +формат: String
        +хэмжээ: Double
        +татах()
        +унших()
        +хадгалах()
    }

    Хэрэглэгч "1" -- "0..*" Захиалга: агуулах
    Хэрэглэгч "1" -- "0..*" Сэтгэгдэл: агуулах
    Хэрэглэгч "1" -- "0..*" Төлбөр: агуулах
    Хэрэглэгч "1" -- "0..*" Мэдэгдэл: агуулах
    Ном "1" -- "0..*" Захиалга: агуулах
    Ном "1" -- "0..*" Сэтгэгдэл: агуулах
    Ном "1" -- "0..1" ЦахимХувь: өргөтгөх
    Админ -- Ном: удирдах
    Админ -- Хэрэглэгч: удирдах
    Админ -- Захиалга: удирдах
