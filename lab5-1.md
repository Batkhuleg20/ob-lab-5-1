# Онлайн номын сангийн системийн класс диаграм

```mermaid
classDiagram
    class Хэрэглэгч {
        -хэрэглэгчийнID: String
        -нэр: String
        -имэйл: String
        -нууц_үг: String
        -утасны_дугаар: String
        -хаяг: String
        +бүртгүүлэх()
        +нэвтрэх()
        +гарах()
        +профайл_шинэчлэх()
    }
    
    class Ном {
        -номынID: String
        -гарчиг: String
        -зохиогч: String
        -хэвлэгдсэн_он: Date
        -ISBN: String
        -төрөл: String
        -тайлбар: String
        -зураг: String
        -байршил: String
        -статус: BooleaN
        +номын_мэдээлэл_харах()
        +номыг_хайх()
    }
    
    class Зээлэлт {
        -зээлэлтийнID: String
        -хэрэглэгчийнID: String
        -номынID: String
        -зээлсэн_огноо: DateTime
        -буцаах_огноо: DateTime
        -бодит_буцаасан_огноо: DateTime
        -торгуулийн_хэмжээ: Double
        -статус: String
        +ном_зээлэх()
        +ном_буцаах()
        +зээлсэн_хугацаа_сунгах()
        +торгууль_тооцох()
    }
    
    class Захиалга {
        -захиалгынID: String
        -хэрэглэгчийнID: String
        -номынID: String
        -захиалсан_огноо: DateTime
        -статус: String
        +ном_захиалах()
        +захиалга_цуцлах()
        +захиалгын_төлөв_шалгах()
    }
    
    class Номын_сан_ажилтан {
        -ажилтныID: String
        -нэр: String
        -албан_тушаал: String
        -холбоо_барих_мэдээлэл: String
        -имэйл: String
        -нууц_үг: String
        +нэвтрэх()
        +гарах()
        +ном_нэмэх()
        +ном_устгах()
        +номын_мэдээлэл_шинэчлэх()
        +зээлэлт_бүртгэх()
        +буцаалт_бүртгэх()
    }
    
    class Үнэлгээ_сэтгэгдэл {
        -үнэлгээID: String
        -хэрэглэгчийнID: String
        -номынID: String
        -үнэлгээ: Integer
        -сэтгэгдэл: String
        -огноо: DateTime
        +үнэлгээ_өгөх()
        +сэтгэгдэл_бичих()
        +үнэлгээ_засах()
        +сэтгэгдэл_устгах()
    }
    
    class Мэдэгдэл {
        -мэдэгдлийнID: String
        -хэрэглэгчийнID: String
        -гарчиг: String
        -агуулга: String
        -огноо: DateTime
        -статус: String
        +мэдэгдэл_үүсгэх()
        +мэдэгдэл_илгээх()
        +мэдэгдэл_уншигдсан_болгох()
    }
    
    class Төлбөр {
        -төлбөрийнID: String
        -хэрэглэгчийнID: String
        -төрөл: String
        -дүн: Double
        -огноо: DateTime
        -статус: String
        +төлбөр_хийх()
        +төлбөрийн_түүх_харах()
        +баримт_хэвлэх()
    }
    
    Хэрэглэгч "1" --> "*" Зээлэлт: makes
    Хэрэглэгч "1" --> "*" Захиалга: creates
    Хэрэглэгч "1" --> "*" Үнэлгээ_сэтгэгдэл: writes
    Хэрэглэгч "1" --> "*" Төлбөр: performs
    Хэрэглэгч "1" --> "*" Мэдэгдэл: receives
    
    Ном "1" --> "*" Зээлэлт: is borrowed in
    Ном "1" --> "*" Захиалга: is ordered in
    Ном "1" --> "*" Үнэлгээ_сэтгэгдэл: has
    
    Номын_сан_ажилтан "1" --> "*" Ном: manages
    Номын_сан_ажилтан "1" --> "*" Зээлэлт: registers
    Номын_сан_ажилтан "1" --> "*" Мэдэгдэл: creates
